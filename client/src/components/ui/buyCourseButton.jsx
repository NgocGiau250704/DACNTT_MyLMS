import React from 'react'
import { Button } from './button'
const buyCourseButton = ({courseId}) => {
  return (
    <div>
      <Button className = "w-full">Purcjase Course</Button>
    </div>
  )
}

export default buyCourseButton
